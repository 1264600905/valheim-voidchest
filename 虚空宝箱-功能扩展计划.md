# 虚空宝箱 功能扩展计划（V2）

- 状态：待评审（描述文案待选择）
- 目标版本：v0.4.0
- 前置：当前 v0.3.2（P0/P1/P2 已完成并测试通过）
- 关联文档：`虚空宝箱-策划案.md`

---

## 1. 概述

本次新增/变更内容一览：

| # | 项目 | 类型 | 说明 |
|---|---|---|---|
| 1 | 每级容量可配置 | 新增设置 | 行/列独立配置，默认保持现状 |
| 2 | 远程存储解锁机制 | 新增设置 + UI 变更 | 默认按箱子等级解锁（L2 起）；单按钮动态切换 |
| 3 | 箱子重量上限 | 新增设置 + 行为 | 仅手动存入受限，支持分堆；附近/远程不受限 |
| 4 | 北欧风格物品描述 | 文案 | 每级多段候选，待选择 |
| 5 | 快捷键 | 维持现状 | 打开热键已可配置，保持不变 |
| 6 | 禁用远程存储 | 维持现状 | `RemoteStore.Enabled` 已存在 |
| 7 | 附近存储应用到普通箱子 | 暂缓 | 本次不做 |

---

## 2. 每级容量可配置

### 决策（已确认）
- 采用 8 个独立整数配置项（每级行、列各一）。
- 默认值与当前一致：`2×6 / 3×6 / 3×8 / 4×8`（行×列）。
- 调小容量后超出显示范围的已有物品：**数据保留但不可见**（与现有低等级裁剪逻辑一致；换大容量物品打开后恢复可见）。

### 配置项

| 配置路径 | 默认 | 范围 |
|---|---|---|
| `Capacity.BlackMetal.Rows` | 2 | 1–8 |
| `Capacity.BlackMetal.Cols` | 6 | 1–8 |
| `Capacity.Magic.Rows` | 3 | 1–8 |
| `Capacity.Magic.Cols` | 6 | 1–8 |
| `Capacity.Flame.Rows` | 3 | 1–8 |
| `Capacity.Flame.Cols` | 8 | 1–8 |
| `Capacity.Crystal.Rows` | 4 | 1–8 |
| `Capacity.Crystal.Cols` | 8 | 1–8 |

### 技术要点
- 修改 `VoidChestManager.GetSize`：由硬编码改为按 prefab 名查配置。
- 数据格式不变（内容仍按最大尺寸序列化保存）。

---

## 3. 远程存储解锁与单按钮 UI

### 决策（已确认）
- 新设置 `RemoteStore.AlwaysAvailable`，**默认 false**：
  - `false`：远程存储需要**当前装备的宝箱等级 ≥ L2（魔能虚空宝箱）**才解锁；
  - `true`：一直可用（初始 L1 即可用远程）。
- **解锁状态与当前装备的箱子等级绑定**（非永久解锁）：拿 L1 打开时按钮是附近存储，拿 L2+ 打开时按钮是远程存入。
- **界面固定一个自定义按钮**（合并现有"附近存储"与"远程存入"两个按钮）：
  - 未解锁：文本"附近存储"，点击执行附近存储；
  - 已解锁：文本"远程存入"，点击执行远程存储。
- `RemoteStore.Enabled`（已有）：总开关，关闭时永远显示"附近存储"（若附近存储启用）。

### 判定逻辑

```
远程可用 = RemoteStore.Enabled && (AlwaysAvailable || 当前装备宝箱等级 >= 2)
按钮文本 = 远程可用 ? "远程存入" : "附近存储"
按钮功能 = 远程可用 ? 远程存储 : 附近存储
按钮隐藏 = (!远程可用 && !附近存储启用)
```

### 注意事项（请评审时确认）
- 合并为单按钮后，**解锁远程时将没有"附近存储"按钮入口**。
  - 备选 1（当前计划）：保持单按钮，解锁后附近存储不再有按钮（可用原版"全部堆叠"替代局部需求）。
  - 备选 2：解锁后按钮执行远程，另加配置 `ShowNearbyButtonWhenRemote`（默认 false）决定是否再显示一个"附近存储"按钮。

### 技术要点
- 等级判断：按装备物品 prefab 名（`VoidChestBlackMetal`=L1，`Magic`=L2，`Flame`=L3，`Crystal`=L4）。
- `VoidChestUi` 改为单按钮动态文本/回调（现有按钮注入框架已支持）。
- 无需持久化解锁状态。

---

## 4. 箱子重量上限

### 决策（已确认）
- 每级重量上限可配置，**默认**：L1=100、L2=150、L3=300、L4=800（单位：负重/kg）。
- 配置范围：1–9999。
- **仅限"手动存入"**（拖拽、Shift 快速移动等）：
  - 存不满时**尝试分堆**：只存入可容纳的数量，剩余留在玩家背包；
  - 重量已满（无法存入任何数量）：**无任何反应**（无提示、无音效，物品原样回弹）。
  - 不显示任何重量预算 UI（保持游戏原生容器重量显示不变）。
- **附近存储与远程存储不涉及重量限制**（批量存入可超过上限）。

### 配置项

| 配置路径 | 默认 | 范围 |
|---|---|---|
| `Weight.BlackMetal.Max` | 100 | 1–9999 |
| `Weight.Magic.Max` | 150 | 1–9999 |
| `Weight.Flame.Max` | 300 | 1–9999 |
| `Weight.Crystal.Max` | 800 | 1–9999 |

### 行为细则

| 场景 | 行为 |
|---|---|
| 拖拽单个物品入箱，剩余额度充足 | 正常存入 |
| 拖拽一堆物品，剩余额度只够一部分 | 存入可容纳数量，其余留在背包（分堆，无提示） |
| 剩余额度不足以存入 1 个 | 无反应（物品回弹） |
| 附近存储 / 远程存储 | 无视重量上限（不检查、不裁剪） |

### 技术要点
- 重量计算：`Inventory.GetTotalWeight()`（容器当前总重）+ `item.m_shared.m_weight`（单位重量）。
- 拦截点：虚拟容器库存的存入方法（`Inventory.MoveItemToThis` 等）。
  - 需要逐一确认所有手动存入路径（拖到空格、拖到已有堆叠、Shift 快速移动）的实际调用方法并统一拦截。
  - 校验公式：`可存数量 = floor((上限 - 容器当前总重) / 单位重量)`，实际存入 = `min(请求数量, 可存数量)`。
  - 完全不可存时返回 false（不执行原型逻辑）。
- 附近/远程的共用堆叠（`VoidChestFilter.StackAllFiltered`）**不加**重量检查。

---

## 5. 北欧风格物品描述（候选，待选择）

> 请从每级 3 段中选择一段（或告诉我组合方式）。

### L1 黑金属虚空宝箱

- **A**：矮人以黑金属为骨、以失落的铭文为魂，封出一角不容于世的虚空。它不属于任何国度，却随你穿越诸界。
- **B**：尼福尔海姆的锻火与密米尔的低语铸成此匣。凡被它吞下之物，皆化为你影子的分量。
- **C**：雾之国的工匠将一片虚空折进黑铁。匣中之物，唯持匣者得以相见。

### L2 魔能虚空宝箱

- **A**：埃达之力在匣面游走，如雾如电。它比铁更能束缚虚空，却仍在等待更深的火焰。
- **B**：你将提炼的埃达精华注入匣体，虚空因此安静下来。它开始记住你的名字。
- **C**：铸入埃达精华后，匣中浮现幽光。诸神早已远去，唯有此匣应你呼唤。

### L3 烈焰虚空宝箱

- **A**：青焰龙王的余烬落入匣面，焚尽了虚空里最后的杂音。此匣所纳，皆为灰烬之上的真实。
- **B**：以焰金属为骨、龙王圣物为约，宝匣在烈火中重铸。它不再只是容器，而是一段被驯服的火焰。
- **C**：火焰舔舐匣缘，却从不吞噬其中之物。青焰认可了你的意志。

### L4 水晶虚空宝箱

- **A**：血金与水霜在匣内交融，凝成一面映照诸界的晶体。虚空在此彻底臣服。
- **B**：最后一枚晶体合拢时，匣中响起极北的风声。你终于拥有了一处不被世界触及的角落。
- **C**：霜与血的造物，诸界之外的门扉。它收纳你的过往，也收纳你尚未走完的路。

---

## 6. 维持现状项

| 项目 | 现状 | 本次处理 |
|---|---|---|
| 快捷键 | 打开/关闭宝箱热键（默认 B）已可在 ConfigurationManager 配置 | 不变（不新增附近/远程热键） |
| 禁用远程存储 | `RemoteStore.Enabled`（默认 true）可关闭远程 | 不变 |
| 附近存储应用到普通箱子 | 目前按钮仅在虚空宝箱界面显示 | **暂缓**，本次不做 |

---

## 7. 配置项变更汇总（v0.4.0）

| 配置节 | 键 | 类型 | 默认 | 新增/变更 |
|---|---|---|---|---|
| Capacity | BlackMetal.Rows / Cols | int (1-8) | 2 / 6 | 新增 |
| Capacity | Magic.Rows / Cols | int (1-8) | 3 / 6 | 新增 |
| Capacity | Flame.Rows / Cols | int (1-8) | 3 / 8 | 新增 |
| Capacity | Crystal.Rows / Cols | int (1-8) | 4 / 8 | 新增 |
| Weight | BlackMetal.Max | float (1-9999) | 100 | 新增 |
| Weight | Magic.Max | float (1-9999) | 150 | 新增 |
| Weight | Flame.Max | float (1-9999) | 300 | 新增 |
| Weight | Crystal.Max | float (1-9999) | 800 | 新增 |
| RemoteStore | AlwaysAvailable | bool | false | 新增 |
| RemoteStore | Enabled | bool | true | 已有（不变） |
| RemoteStore | CacheSeconds | float (0-3600) | 300 | 已有（不变） |
| General | OpenHotkey | KeyCode | B | 已有（不变） |

---

## 8. 实现顺序建议

1. 容量配置（低风险，先落地）
2. 远程解锁 + 单按钮 UI（含 AlwaysAvailable）
3. 重量限制（拖拽拦截 + 分堆，需逐一确认存入路径）
4. 描述文案（待用户选择后写入）
5. 编译部署 + 游戏内验证（容量/解锁按钮/重量分堆/附近远程不受限）
6. 提交 + 更新策划案附录

---

## 9. 待确认

1. **描述文案选择**（§5，每级选 A/B/C 或组合）。
2. 单按钮合并后是否需要在解锁远程时保留"附近存储"入口（§3 备选 2，默认不加）。
3. 重量上限配置项是否需要"0 = 无限制"（当前计划为 1–9999，无 0 语义）。
