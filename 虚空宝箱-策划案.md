# 虚空宝箱（Void Chest）策划案

- 状态：设计定稿（待开发）
- 目标游戏：Valheim 1.0
- 运行环境：BepInEx 5.4.2350 / Jotunn 2.29.2 / .NET Framework 4.8.1
- 参考工程：`BossStaggerFix-src`、`DyrnwynRefresh-src`
- 开发目录：`mymod/`（后续所有开发产物均在此目录下）

---

## 1. 项目概述

新增一条可制作的装备道具链"虚空宝箱"（4 级）：

- 装备后按热键打开一个原版风格的容器界面。
- 每个玩家拥有独立的一份虚空宝箱内容，**数据绑定在玩家角色存档上**（`Player.m_customData`）。
- 宝箱物品只是"钥匙"：丢进任何箱子、交易、掉落、参与合成都不影响数据归属。
- 4 个等级（黑金属 → 魔能 → 烈焰 → 水晶），容量依次 2×6 / 3×6 / 3×8 / 4×8（行×列）。
- 升级方式：在工作台用"上一级宝箱 + 材料"合成下一级宝箱；用低等级物品打开只能看到前几行（数据完整保留）。
- 支持原版"全部堆叠"到宝箱；自研"附近存储"（可选 V+ 兼容）；**远程存入守护石仓库**（利用原版守护石领地范围，在外面跑图时把物资存回家）。

---

## 2. 核心设计决策（已确认）

| 项目 | 决策 |
|---|---|
| 数据归属 | 玩家角色级，与物品无关 |
| 访问前提 | 必须装备宝箱物品才能打开 |
| 物品链 | 黑金属虚空宝箱 → 魔能虚空宝箱 → 烈焰虚空宝箱 → 水晶虚空宝箱（4 个独立 prefab） |
| 容量 | 2×6 / 3×6 / 3×8 / 4×8（行×列，即 12/18/24/32 格） |
| 升级方式 | 工作台配方链：上一级宝箱 + 材料合成下一级（前两级熔炉，后两级黑熔炉） |
| 低等级访问 | 数据不删，只显示当前物品对应的行数 |
| 美术 | 复用希尔德的白银宝箱模型；图标/贴图按 EpicLoot 品质色换色（Epic/Legendary/Mythic/Ancient） |
| 装备位置 | 原版 Utility 槽；检测到 ExtraSlots 时兼容其额外 Utility 槽 |
| 放进其他箱子 | 允许，数据仍留在玩家角色上 |
| V+ 依赖 | 可选；自研"附近存储"优先，启用时屏蔽 V+ AutoStack |
| 功能开关 | "附近存储"与"远程存入"两个独立开关；入口为宝箱界面上的两个按钮（无热键） |
| 远程仓库 | 基于原版守护石（Guard Stone）领地范围，无需自制锚点；需在服务端执行（见 §6） |

---

## 3. 数据模型与持久化（关键结论）

### 3.1 数据归档位置：`Player.m_customData`

反编译验证（assembly_valheim）：

- `Player.m_customData` 是 public 字段：`Dictionary<string, string>`（行 9978）。
- 原版 `Player.Save(ZPackage)` 会写入该字典（行 14151），`Player.Load` 会读回（行 14376）。
- 即：**无需 patch 任何存档方法**，直接读写即可，数据自动随角色 `.fch` 保存/加载。
- 卸载 mod 后数据仍保留在存档中（未知 key 原样读写），重装后即可恢复。
- 联机下与 EpicLoot 冒险数据、ExtraSlots 备份同机制，已被实际使用验证。

这意味着"每个人只能通过宝箱物品打开自己的宝箱"天然成立：
玩家 A 和 B 打开同一件宝箱物品，各自读取的是各自 `Player.m_customData` 中的数据。

### 3.2 数据格式

```
Player.m_customData["liu_VoidChest"] =
    JSON { "version": 1, "data": "<Base64>" }
    Base64 内容 = ZPackage( Inventory.Save )，可选压缩
```

- 存档 key 带命名空间（`liu_` 前缀），避免与其他 mod 冲突。
- 预留 `version` 字段用于日后格式迁移。
- 数据容量预估：最高等级 80 格 × 平均 ~100B ≈ 8KB，Base64 后约 11KB，字符串存档案无压力。

### 3.3 数据与物品解耦

| 关注的实体 | 存储位置 | 是否随物品转移 |
|---|---|---|
| 宝箱内容物 | Player.m_customData | 否 |
| 宝箱等级/容量 | 由物品本身决定（4 个独立 prefab，无需额外数据） | 是（跟物品走） |
| 远程仓库目标 | 无需存储（实时按守护石权限 + 范围扫描） | — |

---

## 4. 功能设计

### 4.1 制作：四级配方链

- 物品类型：`ItemDrop.ItemData.ItemType.Utility`。
- 自定义物品用 Jotunn `CustomItem` 注册；配方用 `RecipeConfig`（指定 `CraftingStation`）。
- 数据与物品解耦：合成消耗上一级宝箱不影响内容（内容在 `Player.m_customData`）。

**配方表**

| 等级 | 物品 | 容量（行×列） | 配方材料 | 工作台 |
|---|---|---|---|---|
| 1 | 黑金属虚空宝箱 | 2×6 = 12 | 5×龙之泪 + 10×细木 + 20×黑金属 | 熔炉 `forge` |
| 2 | 魔能虚空宝箱 | 3×6 = 18 | 1×黑金属虚空宝箱 + 10×埃达精华 + 2×裂魂 | 熔炉 `forge` |
| 3 | 烈焰虚空宝箱 | 3×8 = 24 | 1×魔能虚空宝箱 + 10×焰金属 + 2×青焰龙王圣物 | 黑熔炉 `blackforge` |
| 4 | 水晶虚空宝箱 | 4×8 = 32 | 1×烈焰虚空宝箱 + 10×血金 + 10×液态冰霜 | 黑熔炉 `blackforge` |

**配方材料字段表（已从游戏资源确认）**

| 游戏内简中名 | 本地化 key | prefab 名 | prefab 路径（`valheim_Data` 内逻辑路径） |
|---|---|---|---|
| 龙之泪 | `$item_dragontear` | `DragonTear` | `Assets/GameElements/Items/materials/DragonTear.prefab` |
| 细木 | `$item_finewood` | `FineWood` | `Assets/GameElements/Items/materials/FineWood.prefab` |
| 黑金属 | `$item_blackmetal` | `BlackMetal` | `Assets/GameElements/Items/materials/BlackMetal.prefab` |
| 埃达精华 | `$item_eitr` | `Eitr` | `Assets/GameElements/Items/materials/Eitr.prefab` |
| 裂魂 | `$item_yagluththing` | `YagluthDrop` | `Assets/GameElements/Items/misc/YagluthDrop.prefab` |
| 焰金属 | `$item_flametal` | `Flametal` | `Assets/GameElements/Items/materials/Flametal.prefab` |
| 青焰龙王圣物 | `$item_fader_drop` | `FaderDrop` | `Assets/GameElements/Items/misc/FaderDrop.prefab` |
| 血金 | `$item_gold` | `Gold` | `Assets/GameElements/Items/materials/Gold.prefab` |
| 液态冰霜 | `$item_frozenfuel` | `FrozenFuel` | `Assets/GameElements/Items/materials/FrozenFuel.prefab` |

> 材料 prefab 均位于资源包 `c4210710`（主资源包）。
> 注意：key 与名字不一致的三项 —— 裂魂=`YagluthDrop`、血金=`Gold`（不是 Coins）、埃达精华=`Eitr`。
> 排除项：1.0 新增"潜力熔炉"系统的 `$item_upgrader_tier4/6/7`（神像材料）与本配方无关（已确认不使用）。

**工作台字段**

| 工作台 | prefab 名 | prefab 路径 | 简中名 |
|---|---|---|---|
| 熔炉 | `forge` | `Assets/GameElements/Pieces/forge.prefab` | 熔炉 |
| 黑熔炉 | `blackforge` | `Assets/GameElements/Pieces/blackforge.prefab` | 黑熔炉 |

**自定义物品命名（建议）**

| 物品 | prefab 名（建议） | 本地化 key（建议） |
|---|---|---|
| 黑金属虚空宝箱 | `VoidChestBlackMetal` | `$item_voidchest_blackmetal` |
| 魔能虚空宝箱 | `VoidChestMagic` | `$item_voidchest_magic` |
| 烈焰虚空宝箱 | `VoidChestFlame` | `$item_voidchest_flame` |
| 水晶虚空宝箱 | `VoidChestCrystal` | `$item_voidchest_crystal` |

**已知注意点**

- 合成时若宝箱正装备在 Utility 槽：允许被消耗（**不做提示**），实现时静默清理装备引用，避免悬空异常。
- 上一级宝箱作为配方材料：`Piece.Requirement` 按物品名匹配，数量 1。

### 4.2 装备与打开

- 装备后，目标物品处于"已装备"状态即可（原版 Utility 槽或 ExtraSlots 额外 Utility 槽）。
- 热键（默认 B，可配置）打开/关闭虚空宝箱容器界面。
- 界面使用原版容器 UI（`InventoryGui.Show`），支持拖拽、右键、全部堆叠、全部取出。
- 未装备时按热键给出提示。

### 4.3 容量与升级

**容量等级（行×列，与物品一一对应）**

| 等级 | 物品 | 行数 | 列数 | 总格数 |
|---|---|---|---|---|
| 1 | 黑金属虚空宝箱 | 2 | 6 | 12 |
| 2 | 魔能虚空宝箱 | 3 | 6 | 18 |
| 3 | 烈焰虚空宝箱 | 3 | 8 | 24 |
| 4 | 水晶虚空宝箱 | 4 | 8 | 32 |

- 升级即配方链换物品：工作台合成下一级宝箱，内容数据不变（存在玩家角色上）。
- 数据始终按**最大容量（4×8=32 格）完整保存**；打开界面时按当前物品的行数设置显示高度。
- 低等级物品打开时：只显示前 N 行，超出部分数据保留但不可见、不可操作；换用高等级物品打开即可恢复可见。
- 界面宽度随物品列数变化（6 列 / 8 列），原版 `InventoryGrid` 自适应。

### 4.4 快速堆叠（宝箱自身）

- 原版容器 UI 自带"全部堆叠"：`InventoryGui.OnStackAll()` 直接调用 `m_currentContainer.GetInventory().StackAll(playerInventory)`（assembly_valheim:50613）。
- 虚拟容器不需要额外开发即可使用该按钮。

### 4.5 附近存储（自研，P1）

把背包物资堆进**周围**的箱子（纯物理范围，不需要守护石）。自研实现，不依赖 V+。

**与远程存入的关系**

- "附近存储"与"远程存入（守护石仓库）"是**两个独立开关**，分别对应虚空宝箱界面上的两个按钮（无热键）：
  - "附近存储"按钮：范围默认 10m（可配置）；
  - "远程存入"按钮：目标是玩家有权限的守护石领地内的箱子（§4.6）。
- 两个按钮只在虚空宝箱界面显示，不影响原版容器界面。
- 两者都只做"背包 → 箱子"方向，不移动箱子里的物品。
- 两者可以同时开启，互不冲突（若附近箱子恰好在守护石范围内，物品会优先被就近堆入，结果幂等）。

**行为规则**

- 对周围容器按距离排序，逐个执行原版堆叠（RPC `Container.StackAll()`，联机安全），带超时保护。
- 排除：正在被使用的容器、无权限容器（可选领地检查）、虚空宝箱虚拟容器自身。
- 支持过滤：食物 / 弹药 / 装备等（默认参考 V+ 习惯：忽略装备）。
- 结束时反馈："存入 X 件物品到 Y 个箱子"。

**与 V+ 的优先级**

- 检测到 V+ 且我们的"附近存储"**启用**时：**屏蔽 V+ 的 AutoStack 自动扩散**，统一走我们的实现（满足"优先使用我们自己的，不走 V+"）。
  - 实现：反射定位 V+ 的 `Inventory_StackAll_Patch.Postfix`，Harmony Patch 其 Prefix 返回 false（详见 §5.6）。
  - 反射失败（V+ 版本变更）时降级：日志提示建议关闭 V+ `[AutoStack]`。
- 我们的"附近存储"**关闭**时：不干预 V+，保留 V+ 原有行为。
- 宝箱界面的原版"全部堆叠"按钮始终只堆进虚空宝箱；是否触发 V+ 扩散同样受上述屏蔽规则约束。

### 4.6 守护石仓库（远程存储，核心诉求，P2）

目标：人在野外，把背包物资远程存进"家"里 —— 存进玩家有权访问的**原版守护石（Guard Stone）**领地范围内的所有箱子。利用原版建筑，不需要新增任何内容，也不需要自制"锚点绑定"数据。

**为什么用守护石**

- 原版守护石（`PrivateArea` 组件）自带完整体系：
  - 球形领地范围（原版半径 10m，`m_radius`）；
  - 拥有者（放置者）+ "允许玩家"名单（非拥有者可交互加入/移除，数据存在 ZDO）。
- 玩家是守护石创建者或已被加入（permitted）时，其领地内的箱子即视为该玩家的仓库目标。
- 目标集合实时扫描得出，换世界/换服务器自动重新计算，无过期数据。

**交互设计**

1. 装备虚空宝箱并打开界面，点击"远程存入"按钮：
   1. 扫描世界上所有"玩家拥有或已被加入"的守护石；
   2. 汇总这些守护石领地范围内的所有箱子；
   3. 按原版堆叠规则把背包中可堆叠物品存进这些箱子（背包 → 箱子方向）；
   4. 反馈："存入 X 件物品到 Y 个箱子（Z 处守护石）"。
2. 无匹配守护石/箱子时给出明确提示。
3. 远程取出暂不做（后续扩展）。

**权限与范围规则**

- 有效守护石：玩家是创建者，或在 permitted 名单中。
- 守护石开关（enabled）不影响判定（加入名单只要求当时关闭，加入后启用仍有效）。
- 范围直接使用守护石自带的 `PrivateArea.m_radius`（原版 10m，不开放乘数配置）。
- 目标箱子：带 `Container` 组件的玩家建造容器（木箱/铁箱/黑金属箱/桶等）；**排除船、车**（已确认）。
- 多个有权限的守护石：全部作为目标（实时汇总）。

**技术约束**

- 该功能必须在**服务端**执行（需要全量 ZDO 数据；客户端只有局部数据）：
  - 单机 / 自己主机开服：直接可用。
  - 专用服务器：服务端需要安装同一 mod（客户端发自定义 RPC）。
- 远处的箱子 GameObject 未加载，全程按 ZDO 数据读写，禁止物理搜索（详见 §6）。

### 4.7 允许放入其他箱子

- 宝箱物品可自由丢入任何容器（数据不在物品上，无泄漏/丢失风险）。
- 唯一提示：物品被收起来时无法打开自己的虚空宝箱（需要取回装备）。

### 4.8 美术与配色

**素材来源：希尔德的白银宝箱（已定位）**

| 资源 | 路径 | bundle |
|---|---|---|
| 本地化 key | `$item_chest_hildir2`（"Hildir's Silver Chest" / "希尔德的白银宝箱"） | — |
| prefab | `Assets/world/Props/HildirWagon/chest_hildir2.prefab` | c4210710 |
| 物品图标 | `Assets/GameElements/Items/_icons/chest_hildir2.png` | 6a33a62 |
| 模型（带动画） | `Assets/world/Props/HildirWagon/MarketProps/model/HildirChest_Animated.fbx` | d9b97a3d |
| 模型（静态件） | `HildirChest.obj` / `HildirChestLid.obj` / `HildirChestCollider.obj` | c4210710 |
| 材质 | `Assets/world/Props/HildirWagon/MarketProps/model/HildirChest2_mat.mat` | c4210710 |
| 贴图（漫反射） | `Assets/world/Props/HildirWagon/MarketProps/model/HildirChest2_d.png` | c4210710 |
| 贴图（法线/遮罩） | `HildirChest1_n.png` / `HildirChest1_m.png` | c4210710 |
| 开箱音效 | `Assets/world/Props/HildirWagon/wav/HildirChest_open_01~05.wav` | c4210710 |

**配色方案：EpicLoot 品质色（取最高四档连续递推）**

| 等级 | 物品 | EpicLoot 品质 | 颜色名 | 色值 |
|---|---|---|---|---|
| 1 | 黑金属虚空宝箱 | Epic | Purple | `#d078ff` |
| 2 | 魔能虚空宝箱 | Legendary | Teal | `#18e7a9` |
| 3 | 烈焰虚空宝箱 | Mythic | Orange | `#ffac59` |
| 4 | 水晶虚空宝箱 | Ancient（远古） | Red | `#ff4545` |

> 色值取自 EpicLoot `ItemColors` 配置（当前 profile 实际值）。
> EpicLoot 完整序列：Magic `#00abff` / Rare `#ffff75` / Epic `#d078ff` / Legendary `#18e7a9` / Mythic `#ffac59` / Ancient `#ff4545`。
> 本方案取最高四档（Epic→Ancient），已确认采用。

**换色实现（已确认：运行时换色）**

1. 运行时：克隆白银宝箱图标 Sprite 与材质副本，按目标色做着色/色相调整（`Material._Color`、图标像素着色），Jotunn `CustomItem` 直接使用，不打包额外资源。
2. 模型复用：克隆 `chest_hildir2` 的模型子树，按装备物品尺寸缩放；物品类型 `Utility`，装备后挂在角色身上。

---

## 5. 技术实现方案

### 5.1 虚拟容器（核心难点）

原版 `Container` 强依赖 `ZNetView`（`Awake` 中 `m_nview.GetZDO()`），因此需要一个"无网络虚拟容器"：

- 创建一个隐藏 GameObject，挂 `VirtualContainer : Container`（不挂 ZNetView、不放 piece 层、不带 Piece）。
- 该对象每帧跟随玩家（满足 `InventoryGui.m_autoCloseDistance = 4m` 距离检查）。
- 不进入 V+ 的附近搜索（V+ 只搜 piece/item/vehicle 层且要求 `Piece.IsPlacedByPlayer()`），互不干扰。

### 5.2 Harmony Patch 清单（针对 VirtualContainer 定向生效）

| 目标方法 | 处理 | 原因 |
|---|---|---|
| `Container.Awake` | Prefix 跳过原逻辑，手动注入 Inventory | 无 ZNetView 会 NRE |
| `Container.IsOwner` | 返回 true | UI 每帧调用 |
| `Container.SetInUse` | 跳过/本地维护 | Hide/Close 时调用 |
| `Container.IsInUse` | 返回本地状态 | UI/V+ 可能调用 |
| `Container.CheckForChanges` | 跳过 | 原逻辑依赖 ZNetView |
| `Player.Save` | Prefix flush 当前宝箱数据 | 防存档时未落盘 |

> `GetInventory()` 无网络依赖，不需要处理。UI 的物品拖拽/堆叠全部是纯 `Inventory` 操作，天然可用。

### 5.3 装备检测

```
IsVoidChestEquipped(player):
  1) player.GetInventory().GetEquippedItems() 中存在虚空宝箱 → true（原版 Utility 槽）
  2) 检测到 ExtraSlots 插件时：
     反射调用 ExtraSlots.ExtraUtilitySlots.GetEquippedItems(player)
     （该方法为 public static，反射调用；失败则降级并记录日志）
  3) 兜底：反射读取 player.m_utilityItem
```

- ExtraSlots 额外槽物品不在原版 `Inventory` 列表中，必须走其 API（反编译确认：`public static List<ItemData> GetEquippedItems(Humanoid)`）。
- 依赖方式：软依赖（检测插件 GUID `shudnal.ExtraSlots`，不强制安装）。

### 5.4 存档读写时机

- 加载：打开界面时（或玩家加载后首次需要时）从 `m_customData` 反序列化。
- 保存：
  - 关闭界面时；
  - 物品变动节流保存（例如 2 秒）；
  - `Player.Save` Prefix 强制 flush（关键保险）。
- 序列化使用原版 `Inventory.Save/Load(ZPackage)`，保证物品数据（含 EpicLoot 词条）完整兼容。

### 5.5 等级与显示行数

- 等级由物品 prefab 决定（4 个独立 prefab），无需物品自定义数据。
- 映射表：`VoidChestBlackMetal→2行` / `VoidChestMagic→3行` / `VoidChestFlame→3行·8列` / `VoidChestCrystal→4行·8列`。
- 打开时按映射设置虚拟容器 Inventory 的显示高度与宽度（数据内部保持最大 32 格，不清除）。
- 合成下一级由 Jotunn 配方负责；消耗上一级物品不影响角色数据。

### 5.6 附近存储实现要点

- 入口：虚空宝箱界面上的"附近存储"按钮（见 §5.7），**无热键**。
- 搜索：`Physics.OverlapSphere(player.pos, range, piece/item/vehicle 层)`，按距离排序。
- 过滤：`GetComponentInParent<Container>()`、`GetInventory() != null`、非 in-use、非虚拟宝箱、权限检查（可选）。
- 执行：串行队列，对每个容器调用原版 `Container.StackAll()`（RPC_RequestStack → 授权 → RPC_StackResponse 中执行堆叠）；patch `Container.RPC_StackResponse` 捕获回调推进队列；单容器超时（默认 2s）后跳过。
- 统计：记录背包物品数前后差值 + 成功容器数，统一提示。
- V+ 屏蔽门控：
  - 启用自研附近存储且检测到 V+ 时，反射 `AccessTools.TypeByName("ValheimPlus.Inventory_StackAll_Patch")` 与其 `Postfix` 方法，Harmony Patch 一个 Prefix（返回 false 阻止 V+ 逻辑）；
  - 全过程 try-catch，失败只记录日志并提示手动关闭 V+ `[AutoStack]`。

### 5.7 宝箱界面按钮注入

- 原版容器按钮为 public 字段：`InventoryGui.m_stackAllButton` / `m_takeAllButton` / `m_dropButton`（`onClick` 绑定见 assembly_valheim:405-410）。
- 实现：虚空宝箱界面打开时（`InventoryGui.Show` 之后），克隆 `m_stackAllButton` 的 GameObject（保留原版样式与交互），插入容器按钮区，绑定两个回调：
  - "附近存储" → 附近存储队列（§5.6）；
  - "远程存入" → 守护石仓库扫描（§6.2）。
- 显示条件：仅当当前打开的是虚空宝箱时显示；对应功能开关关闭的按钮隐藏。
- 按钮文本走本地化；关闭/切换界面时恢复原状，不残留到原版容器界面。

---

## 6. 远程堆叠技术专题

### 6.1 为什么 V+ 的附近堆叠无法远程

反编译 ValheimPlus v10.1.0 确认：

- `InventoryAssistant.GetNearbyChests()` 使用 `Physics.OverlapSphere`（物理范围搜索，配置上限 50m）。
- 触发方式是 patch `Inventory.StackAll` 的后置扩散，**没有可供外部调用的 API**（内部方法为 internal/private static async）。
- 结论：V+ 只能"周围堆叠"，**做不到远程**；远程必须自研。

### 6.2 守护石远程仓库方案（自研）

**数据基础（反编译已验证）**

- 服务端 `ZDOMan` 内存持有全部 ZDO（分 sector 存储），并提供公开 API：
  - `GetAllZDOsWithPrefabIterative(prefab, list, ref index)`（行 77473）：按 prefab 名分帧迭代全量 ZDO（每轮约 400 个 sector），不卡主线程；
  - `GetZDO(ZDOID)`（行 76785）：按 ID 直接取；
  - `ZNetScene.GetPrefab(hash/name)`（行 82171/82180）：由 prefab 哈希反查 prefab，按组件类型（`PrivateArea`/`Container`）识别，不硬编码名称。
- 守护石权限数据就在其 ZDO 上（原版 `PrivateArea` 写入）：
  - `permitted`（int，名单数量）、`pu_id{i}`（long）、`pu_name{i}`（string）；
  - 创建者：Piece 的 creator 字段。
- 箱子内容物：ZDO 字段 `items`（byte[]），即 `Inventory.Save(ZPackage)` 的原始字节，可直接反序列化/写回。

**执行流程（主机模式 / 单机）**

1. 分帧扫描所有守护石 ZDO（按 `PrivateArea` 组件识别 prefab）。
2. 过滤出玩家有权限的守护石（creator == playerID，或 permitted 名单包含 playerID）。
3. 取每个守护石位置 + prefab 半径 `m_radius`。
4. 分帧扫描容器 ZDO（按 `Container` 组件识别 prefab），位置落在任一守护石半径内 → 目标箱子。
5. 对每个目标箱子：`items` → ZPackage → `Inventory.Load` → `StackAll(playerInventory)` → `Inventory.Save` → 写回 ZDO。
6. 本地玩家背包（主机权威）移除已存入物品，统计并提示。

**执行流程（专用服务器）**

1. 客户端 → 服务端 RPC：请求执行远程存入（携带玩家 ID 与背包物品数据）。
2. 服务端执行上述 1~5（服务端 ZDO 权威），返回实际移动结果。
3. 客户端按结果移除背包物品。

**分阶段落地**

| 阶段 | 范围 | 说明 |
|---|---|---|
| P2-a | 单机/主机 | 本地直接执行，最快可用 |
| P2-b | 专用服务器 | 增加自定义 RPC 与服务端校验 |

**边界与风险**

- 服务端需要装 mod（写入 ServerSync 说明）。
- 分帧扫描：全量 ZDO 数量大，必须使用迭代 API，禁止一次性遍历导致掉帧。
- 箱子被拆/移动：ZDO 失效自动跳过。
- 反作弊：服务端只接受"向箱子增加物品"的变更；单机无此问题。
- 远程取出（从家里拿东西）暂不做，作为后续扩展。

---

## 7. 兼容性矩阵

| 依赖/mods | 关系 | 说明 |
|---|---|---|
| BepInEx 5.4.2350 | 必需 | 运行环境 |
| Jotunn 2.29.2 | 必需 | 自定义物品、配方、本地化、RPC |
| Valheim Plus | 可选 | 自研附近存储优先；启用时屏蔽 V+ AutoStack，关闭时保留 V+ 行为 |
| ExtraSlots | 可选 | 额外 Utility 槽装备；反射检测 |
| EpicLoot | 无冲突 | 宝箱内容物通过 Inventory 原版序列化，词条兼容；配色方案参考其品质色配置（§4.8） |
| ConfigurationManager | 可选 | 配置界面 |
| 多人 | 支持 | 数据按玩家角色各存各的；守护石远程仓库需服务端装 mod（见 §6） |

---

## 8. 风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| 虚拟容器 patch 点被原版更新改动 | 中 | 集中在一个 Patches 类，失败时禁用功能并日志提示 |
| ExtraSlots 反射接口变更 | 低 | try-catch 降级到原版槽检测 |
| 远程功能在专用服务器不可用 | 中 | 明确说明需服务端装 mod；P2-a 先支持主机 |
| 玩家用低 Tier 物品访问时产生"看不见的物品" | 低 | 设计预期行为；UI 上提示"更高等级可显示更多行" |
| 宝箱物品与 EpicLoot 的 utility 词条交互 | 低 | 开发时实测（Utility 槽装备路径） |
| 屏蔽 V+ AutoStack 依赖其内部类名（反射） | 低 | try-catch 降级 + 提示用户关闭 V+ `[AutoStack]` |

---

## 9. 开发阶段计划

**P0 — 基础可用（MVP）**

1. 物品注册（Jotunn）：4 级虚空宝箱（Utility 类型，白银宝箱模型/图标换色）。
2. 配方链注册（熔炉/黑熔炉，材料字段见 §4.1）。
3. 虚拟容器 + Harmony patch。
4. `Player.m_customData` 读写（含版本头）。
5. 装备检测（原版槽）+ 打开热键（默认 B，可配置）。
6. 原版容器 UI 复用（堆叠/取出可用）+ 等级显示裁剪。
7. 配置与中文本地化。

**P1 — 附近存储与兼容**

8. ExtraSlots 软依赖检测。
9. 自研附近存储（独立开关 + 队列/超时 + 过滤）。
10. 宝箱界面按钮注入（§5.7："附近存储" / "远程存入"）。
11. V+ AutoStack 屏蔽门控（启用自研功能时）。

**P2 — 守护石远程仓库**

12. 守护石/箱子 ZDO 分帧扫描 + 权限过滤。
13. 主机模式远程存入（含统计反馈）。
14. 专用服务器 RPC 方案。
15. （扩展）远程取出。

---

## 10. 补充决定（已确认）

| # | 事项 | 决定 |
|---|---|---|
| 1 | 配色档位 | 采用 EpicLoot 最高四档：Epic / Legendary / Mythic / Ancient（远古红） |
| 2 | 换色实现 | 运行时换色（不打包额外资源） |
| 3 | 装备中参与合成 | 不做提示，允许消耗；静默清理装备引用 |
| 4 | V+ AutoStack 同时触发 | 可接受，无需额外开关 |
| 5 | 守护石仓库 | 排除船/车；范围用守护石默认 `m_radius`；多个有权限守护石全部作为目标 |
| 6 | 触发方式 | 不使用热键；"附近存储"与"远程存入"均为宝箱界面按钮 |

---

## 附录：关键反编译证据

| 机制 | 位置 | 说明 |
|---|---|---|
| `Player.m_customData` 字段 | assembly_valheim 行 9978 | public，角色级自定义数据 |
| `Player.Save` 写入 customData | 行 14151 | 自动随 .fch 持久化 |
| `Player.Load` 读取 | 行 14376 | 未知 key 原样保留 |
| 容器堆叠 UI | `InventoryGui.OnStackAll` 行 50613 | 纯 Inventory 操作 |
| 自动关闭距离 | `InventoryGui.m_autoCloseDistance = 4f` 行 49852 | 虚拟容器跟随玩家即可 |
| ExtraSlots 额外槽 API | `ExtraUtilitySlots.GetEquippedItems`（ExtraSlots.dll） | public static，反射可用 |
| V+ 附近堆叠 | `InventoryAssistant.GetNearbyChests`（ValheimPlus.dll） | 物理范围，最大 50m，无法远程 |
| 守护石权限数据 | `PrivateArea.SetPermittedPlayers/GetPermittedPlayers` 行 137252/137263 | ZDO：`permitted`/`pu_id{i}`/`pu_name{i}` |
| 守护石概念 | `PrivateArea` 行 137051 | `m_radius` 默认 10m，含 creator/permitted 体系 |
| 全量 ZDO 扫描 API | `ZDOMan.GetAllZDOsWithPrefabIterative` 行 77473 | public，分帧迭代，服务端可用 |
| prefab 反查 | `ZNetScene.GetPrefab(hash/name)` 行 82171/82180 | 按组件识别容器/守护石 |
| EpicLoot 品质色定义 | `EpicLoot.MagicItemColors`（EpicLoot.dll） | Red `#ff4545` / Orange `#ffac59` / Yellow `#ffff75` / Green `#80fa70` / Teal `#18e7a9` / Blue `#00abff` / Indigo `#709bba` / Purple `#d078ff` / Pink `#ff63d6` / Gray `#dbcadb` |
| 资源索引 | `valheim_Data/StreamingAssets/SoftRef/manifest_extended` | 资源路径 → bundle 映射（1.0 资源系统） |

## 附录 B：物品/资源字段速查（1.0 实机资源）

**配方材料（9 项）**

| 简中名 | 本地化 key | prefab | 路径 | bundle |
|---|---|---|---|---|
| 龙之泪 | `$item_dragontear` | `DragonTear` | `Assets/GameElements/Items/materials/DragonTear.prefab` | c4210710 |
| 细木 | `$item_finewood` | `FineWood` | `Assets/GameElements/Items/materials/FineWood.prefab` | c4210710 |
| 黑金属 | `$item_blackmetal` | `BlackMetal` | `Assets/GameElements/Items/materials/BlackMetal.prefab` | c4210710 |
| 埃达精华 | `$item_eitr` | `Eitr` | `Assets/GameElements/Items/materials/Eitr.prefab` | c4210710 |
| 裂魂 | `$item_yagluththing` | `YagluthDrop` | `Assets/GameElements/Items/misc/YagluthDrop.prefab` | c4210710 |
| 焰金属 | `$item_flametal` | `Flametal` | `Assets/GameElements/Items/materials/Flametal.prefab` | c4210710 |
| 青焰龙王圣物 | `$item_fader_drop` | `FaderDrop` | `Assets/GameElements/Items/misc/FaderDrop.prefab` | c4210710 |
| 血金 | `$item_gold` | `Gold` | `Assets/GameElements/Items/materials/Gold.prefab` | c4210710 |
| 液态冰霜 | `$item_frozenfuel` | `FrozenFuel` | `Assets/GameElements/Items/materials/FrozenFuel.prefab` | c4210710 |

**工作台（2 项）**

| 简中名 | prefab | 路径 | bundle |
|---|---|---|---|
| 熔炉 | `forge` | `Assets/GameElements/Pieces/forge.prefab` | c4210710 |
| 黑熔炉 | `blackforge` | `Assets/GameElements/Pieces/blackforge.prefab` | c4210710 |

**白银宝箱（美术素材）**

| 资源 | 路径 | bundle |
|---|---|---|
| prefab | `Assets/world/Props/HildirWagon/chest_hildir2.prefab` | c4210710 |
| 图标 | `Assets/GameElements/Items/_icons/chest_hildir2.png` | 6a33a62 |
| 模型 | `Assets/world/Props/HildirWagon/MarketProps/model/HildirChest_Animated.fbx` | d9b97a3d |
| 材质 | `Assets/world/Props/HildirWagon/MarketProps/model/HildirChest2_mat.mat` | c4210710 |
| 贴图 | `Assets/world/Props/HildirWagon/MarketProps/model/HildirChest2_d.png` | c4210710 |

**干扰项（不使用）**

| 物品 | 本地化 key | prefab | 说明 |
|---|---|---|---|
| 黑金属神像材料 | `$item_upgrader_tier4` | `Upgrader4Armor/Weapon` | 同名"黑金属"，潜力熔炉系统 |
| 焰金属神像材料 | `$item_upgrader_tier6` | `Upgrader6Armor/Weapon` | 同名"焰金属" |
| 血金神像材料 | `$item_upgrader_tier7` | `Upgrader7Armor/Weapon` | 同名"血金" |
