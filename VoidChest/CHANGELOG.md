# 0.4.1

* 修正 L3 烈焰虚空宝箱的配方材料：`Flametal` → `FlametalNew`
  （Valheim 1.0 中"焰金属"对应的 prefab 是 FlametalNew；旧 Flametal 已更名为"上古金属"，导致配方永远无法解锁）

# 0.4.0

* 新增：每级容量可配置（行/列，1-8）
* 新增：每级重量上限可配置（默认 100/150/300/800，0 = 无限制）
* 新增：重量显示为"当前/上限"
* 新增：手动存入（拖拽 / Shift 快速移动）超重时自动分堆，重量已满时无反应；附近/远程存储不受重量限制
* 新增：远程存储解锁机制（`RemoteStore.AlwaysAvailable`，默认关闭 = 装备魔能/L2 及以上宝箱解锁）
* 变更：界面合并为单按钮（未解锁显示"附近存储"，解锁后显示"远程存入"）
* 新增：物品描述（北欧魔法风格）
* 新增：三语言本地化（English / 简体中文 / 繁體中文）
* 新增：仓库与发布文件（README / manifest / icon / LICENSE）

# 0.3.2

* 性能：远程存储扫描改为全量快照分类（2006 批 → 1 批），扫描耗时从 1.4-1.7 秒降至 ~40ms
* 性能：分类阶段预读 prefab hash，避免 100 万次 ZDO 字典查询
* 性能：帧预算 4ms → 8ms；增加分阶段性能统计（快照/分类/筛选/堆叠/数据写入）

# 0.3.1

* 修复：远程存储只保存 ZDOID，消除 ZDO 对象池复用导致的数据污染风险

# 0.3.0

* 新增：P2 守护石远程仓库（单机/主机模式）
* 扫描玩家有权限（创建者 / permitted 名单）的守护石，远程存入其领地范围内的所有箱子
* 排除船/车/墓碑；跳过使用中的箱子
* 全量 ZDO 数据操作（远处箱子无需加载）

# 0.2.0

* 新增：自研"附近存储"（可配置范围、过滤规则、串行队列 + 超时）
* 新增：宝箱界面按钮注入
* 新增：ExtraSlots 软依赖（额外 Utility 槽装备检测与专用槽位过滤）
* 新增：Valheim Plus AutoStack 门控（启用附近存储时屏蔽 V+ 扩散）

# 0.1.0

* 初始版本（P0）：
* 四级虚空宝箱物品链（黑金属 / 魔能 / 烈焰 / 水晶）
* 角色级数据（`Player.m_customData`），跨世界携带；物品仅作为钥匙
* 虚拟容器 + 原版容器 UI（堆叠 / 取出可用）
* 容量随等级裁剪显示；低等级打开数据保留
* 配方链（熔炉 / 黑熔炉）、图标与材质运行时换色
